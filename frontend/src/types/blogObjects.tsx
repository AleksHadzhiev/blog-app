export type CreateBlog = {
    title: string,
    description: string,
}

export type EditBlog = {
    title: string,
    description: string,
}

export type Blog = {
    title: string,
    description: string,
    // type: string,
    // shortDescription: string,
}